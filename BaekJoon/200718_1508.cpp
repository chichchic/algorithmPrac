#include <iostream>
#include <vector>
/*
못풀었습니다.
*/
using namespace std;

int main()
{
  freopen("input.txt", "r", stdin);
  int n, m, k;
  cin >> n >> m >> k;
  vector<int> position(k);
  for (int i = 0; i < k; i++)
  {
    cin >> position[i];
  }

  return 0;
}