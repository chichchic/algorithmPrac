#include <iostream>
#include <cmath>

using namespace std;

int main()
{
  freopen("input.txt", "r", stdin);

  long long n, r, c;
  cin >> n >> r >> c;
  long long len = (long long)pow(2.0, (double)n);
  long long ans = 0;
  while (r >= 2)
  {
    long long size = 2;

  }
  return 0;
}